{
  "name": "aeonic-governance-suite",
  "version": "1.0.0",
  "description": "Gobernanza ética y autocorrección on-chain (AeonicAgent + Safe + IPFS).",
  "type": "commonjs",
  "scripts": {
    "build": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy-agent.js --network sepolia",
    "verify": "node scripts/verify-agent.js",
    "propose": "node scripts/propose-safe-tx.js",
    "monitor": "node scripts/monitor-safe-tx.js",
    "safe-status": "node scripts/fetch-safe-status.js"
  },
  "dependencies": {
    "@safe-global/api-kit": "^1.2.0",
    "@safe-global/protocol-kit": "^1.3.0",
    "axios": "^1.7.4",
    "ethers": "^6.11.1",
    "fs-extra": "^11.2.0",
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.2",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.2",
    "mocha": "^10.7.3"
  }
}